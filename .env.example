# HourMail Environment Configuration
# Copy this file to .env and adjust values as needed

# ── Database (PostgreSQL) ──────────────────────────────
POSTGRES_USER=hourmail
POSTGRES_PASSWORD=hourmail_secret
POSTGRES_DB=hourmail

# Connection URL for the app (used for local dev outside Docker)
# Replace <SERVER_IP> with your server's IP if not running locally
# Port 5487 is the exposed Postgres port from docker-compose
# NOTE: URL-encode special characters in the password (e.g. % -> %25)
DATABASE_URL=postgresql://hourmail:hourmail_secret@<SERVER_IP>:5487/hourmail

# ── Redis ──────────────────────────────────────────────
REDIS_PASSWORD=redis_secret

# Connection URL for the app (used for local dev outside Docker)
# Replace <SERVER_IP> with your server's IP if not running locally
# Port 6391 is the exposed Redis port from docker-compose
REDIS_URL=redis://default:redis_secret@<SERVER_IP>:6391

# ── App ────────────────────────────────────────────────
NUXT_PORT=3847
NUXT_SESSION_SECRET=change-me-to-a-random-64-char-string
